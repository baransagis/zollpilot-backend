ktor {
  application {
    modules = [ com.zollpilot.ApplicationKt.module ]
  }
  deployment {
    port = 8080
  }
}

app {
  testCsvFile = "materials.csv"
  catalog {
    familiesPath = "catalog/families.json"
    candidatesPath = "catalog/cn-candidates.json"
  }
}
